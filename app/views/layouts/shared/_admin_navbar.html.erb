<div class="grid grid-cols-1">
  <!-- Navbar -->
  <nav class="bg-gray-800 p-4" data-controller="menu">
    <div class="grid grid-cols-12">
      <div class="col-start-1">
        <a href="#" class="text-white text-2xl font-bold">Admin Panel</a>
      </div>
      <div class="col-start-3 col-end-5 col-span-2">
        <div class="hidden md:flex justify-start space-x-4">
          <%= button_to 'Companies', companies_path, method: :get, class: "text-white px-3 py-2 rounded-md text-sm font-medium #{current_page?(companies_path) ? 'bg-gray-700' : 'hover:bg-gray-700'}", data: { turbo_frame: 'tab-content' } %>
          <%= button_to 'Agencies', agencies_path, method: :get, class: "text-white px-3 py-2 rounded-md text-sm font-medium #{current_page?(agencies_path) ? 'bg-gray-700' : 'hover:bg-gray-700'}", data: { turbo_frame: 'tab-content' } %>
        </div>
      </div>

      <div class="md:hidden">
        <button data-action="click->menu#toggle" class="text-white hover:bg-gray-700 p-2 rounded-md">
          <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" class="w-6 h-6">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
          </svg>
        </button>
      </div>
    </div>

    <div class="md:hidden hidden" data-menu-target="menu">
      <div class="px-2 pt-2 pb-3 space-y-1 sm:px-3">
        <%= button_to 'Companies', companies_path, method: :get, class: "text-white px-3 py-2 rounded-md text-sm font-medium #{current_page?(companies_path) ? 'bg-gray-700' : 'hover:bg-gray-700'}", data: { turbo_frame: 'tab-content' } %>
        <%= button_to 'Agencies', agencies_path, method: :get, class: "text-white px-3 py-2 rounded-md text-sm font-medium #{current_page?(agencies_path) ? 'bg-gray-700' : 'hover:bg-gray-700'}", data: { turbo_frame: 'tab-content' } %>
      </div>
    </div>
  </nav>
</div>